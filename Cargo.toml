[package]
name = "hn-app"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.33.3"
reqwest = { version = "0.12", features = ["json", "blocking"] }
serde = { version = "1", features = ["derive"] }
